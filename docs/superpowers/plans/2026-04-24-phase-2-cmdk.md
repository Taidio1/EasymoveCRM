# Phase 2 — Cmd+K Global Search Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement a global command menu (⌘K) for quick navigation, client search, and common actions, matching the Claude Design "Wariant A" aesthetic.

**Architecture:** Create a `CommandMenu` component using `cmdk` and Shadcn `Command` primitives. Integrate it into `AppProviders` to make it accessible globally. Implement search logic for clients using Supabase and static lists for navigation and actions.

**Tech Stack:** Next.js 15, React 19, `cmdk`, Lucide React, Supabase.

---

## File structure

**Modify:**
- `app/AppProviders.tsx` — Register the global `CommandMenu` component.
- `components/ui/command.tsx` — Ensure styling matches design tokens (already mostly done in Phase 0, but may need minor tweaks for `surface-raised`).
- `components/sidebar.tsx` — Wire the search button to trigger the command menu.

**Create:**
- `components/command-menu.tsx` — The main implementation of the ⌘K menu.

---

## Task 1: Create CommandMenu Component

**Files:**
- Create: `components/command-menu.tsx`

- [ ] **Step 1: Implement basic dialog structure and keyboard listener**

Implement the state and `useEffect` to listen for `Meta+K` or `Ctrl+K`.

```tsx
"use client"

import * as React from "react"
import {
  LayoutDashboard,
  Users,
  Calendar,
  BarChart3,
  FileText,
  Plus,
  Moon,
  Sun,
  Laptop
} from "lucide-react"
import { useRouter } from "next/navigation"
import { useTheme } from "next-themes"

import {
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
} from "@/components/ui/command"

export function CommandMenu() {
  const [open, setOpen] = React.useState(false)
  const router = useRouter()
  const { setTheme } = useTheme()

  React.useEffect(() => {
    const down = (e: KeyboardEvent) => {
      if (e.key === "k" && (e.metaKey || e.ctrlKey)) {
        e.preventDefault()
        setOpen((open) => !open)
      }
    }
    document.addEventListener("keydown", down)
    return () => document.removeEventListener("keydown", down)
  }, [])

  return (
    <CommandDialog open={open} onOpenChange={setOpen}>
      <CommandInput placeholder="Wpisz polecenie lub szukaj..." />
      <CommandList>
        <CommandEmpty>Brak wyników.</CommandEmpty>
        <CommandGroup heading="Nawigacja">
          <CommandItem onSelect={() => { router.push("/"); setOpen(false) }}>
            <LayoutDashboard />
            <span>Pulpit</span>
          </CommandItem>
          <CommandItem onSelect={() => { router.push("/clients"); setOpen(false) }}>
            <Users />
            <span>Klienci</span>
          </CommandItem>
          <CommandItem onSelect={() => { router.push("/calendar"); setOpen(false) }}>
            <Calendar />
            <span>Terminy</span>
          </CommandItem>
        </CommandGroup>
        <CommandSeparator />
        <CommandGroup heading="Motyw">
          <CommandItem onSelect={() => setTheme("light")}>
            <Sun />
            <span>Jasny</span>
          </CommandItem>
          <CommandItem onSelect={() => setTheme("dark")}>
            <Moon />
            <span>Ciemny</span>
          </CommandItem>
          <CommandItem onSelect={() => setTheme("system")}>
            <Laptop />
            <span>Systemowy</span>
          </CommandItem>
        </CommandGroup>
      </CommandList>
    </CommandDialog>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add components/command-menu.tsx
git commit -m "feat(ui): implement base CommandMenu with navigation and theme toggle"
```

---

## Task 2: Implement Client Search from Supabase

**Files:**
- Modify: `components/command-menu.tsx`

- [ ] **Step 1: Add client search logic**

Implement debounced search for clients using `supabase.from('clients').select().ilike('Name', ...)`.

- [ ] **Step 2: Add Client Results section to the menu**

Display found clients with their ID and country flag (if available).

- [ ] **Step 3: Commit**

```bash
git add components/command-menu.tsx
git commit -m "feat(ui): add client search to CommandMenu"
```

---

## Task 3: Global Integration and Triggers

**Files:**
- Modify: `app/AppProviders.tsx`
- Modify: `components/sidebar.tsx`

- [ ] **Step 1: Add CommandMenu to AppProviders**

```tsx
export function AppProviders({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider ...>
      <AuthProvider>
        <AuthGuard>
          <SidebarProvider>
            {children}
            <CommandMenu />
          </SidebarProvider>
        </AuthGuard>
      </AuthProvider>
    </ThemeProvider>
  )
}
```

- [ ] **Step 2: Wire sidebar search button**

Export a way to trigger the command menu from `CommandMenu` or use a custom event/state if needed. Alternatively, use a simple `window.dispatchEvent` or just listen for the click in `sidebar.tsx` and trigger the same state (may need to move `open` state to a context if we want programmatic trigger from sidebar).

*Decision: Move CommandMenu state to a small context or just use the keyboard shortcut simulation for the button.*

Actually, `cmdk` doesn't have a built-in "trigger" besides standard state. Let's create a small `CommandMenuProvider` or just use the search button in Sidebar to dispatch the same keyboard event.

- [ ] **Step 3: Commit**

```bash
git add app/AppProviders.tsx components/sidebar.tsx
git commit -m "feat(ui): integrate CommandMenu globally and wire sidebar trigger"
```

---

## Task 4: Final Polish and Styling

**Files:**
- Modify: `components/ui/command.tsx`

- [ ] **Step 1: Apply Claude Design styling to Command components**

Ensure `CommandDialog` uses `bg-surface-raised`, correct padding, and font sizes (`text-sm` for items, `text-xs` for shortcuts).

- [ ] **Step 2: Verify Build and Functionality**

Run `pnpm build` and verify `⌘K` works on all pages and sidebar button opens it.

- [ ] **Step 3: Commit**

```bash
git add components/ui/command.tsx
git commit -m "style(ui): polish command menu styling per design tokens"
```
